# Задаем параметры распределения Бернулли
p <- 0.5 # вероятность успеха

# Генерируем случайную выборку размера n, соответствующую распределению Бернулли
n <- 100 # размер выборки
samples <- rbinom(n, 1, p) # генерация выборки

# Вычисляем выборочную долю успехов и ее стандартное отклонение
phat <- mean(samples) # выборочная доля успехов
se <- sqrt(phat*(1-phat)/n) # стандартное отклонение выборочной доли

# Выводим результаты
cat("Выборочная доля успехов: ", phat, "\n")
cat("Стандартное отклонение выборочной доли: ", se, "\n")
